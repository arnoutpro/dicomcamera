org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# AGP 9's new DSL (default-on) is incompatible with the explicitly-applied
# org.jetbrains.kotlin.android plugin used by this project. Opt out of the
# new DSL rather than migrating to AGP's built-in Kotlin support, which
# would be a separate, larger change.
android.newDsl=false
