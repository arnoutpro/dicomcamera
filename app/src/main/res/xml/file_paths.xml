<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the EXTRA_OUTPUT target is shared via FileProvider.
         Staging / pending / local-archive stay app-private and must not be exposable. -->
    <external-files-path name="ext_pictures_capture" path="Pictures/capture/" />
</paths>
